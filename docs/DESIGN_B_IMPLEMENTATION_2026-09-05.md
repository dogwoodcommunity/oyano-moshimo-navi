# B案の実装・確認 — 2026年9月5日

## 状態

ユーザーが比較案B「迷わない3つの入口」を選択したため実装。
ブランチは `design/readable-three-entry`、開始点は `main` の `3206a6b`。
**ローカル確認版。本番未反映。商用版全体の完成や実機受入完了を意味しない。**

## 変えたところ

- 書体：Nextが配信するZen Maru Gothic（500/700）。ロゴの字体は従来どおり。
- 配色：生成りの地に白い記録、濃い茶色の操作、本文はほぼ黒。新テーマをlegacy CSSと分離。
- 主な入口：「記録を書く」「記録を見返す」「AIに相談する」の3行。手順の強制と誤解されないよう番号ではなくアイコンを付けた。
- 入口の次に最新記録を置き、家族共有・書類の場所は補助リンクへ。既存6ページのタブは残す。
- 大きな手帳表紙・重複ロゴ・繰り返しの説明を小さい人物切替とプロフィールリンクに整理。対象者名・関係・状況は手帳見出しに維持。
- 記録本文は20px/700、主要見出し28px、主要入口22〜24px、入口補足16px。保存ボタンは高さ56px以上、補助操作48px以上を基本にした。
- 記録カードとAIヒントの入れ子色・影を簡素化。AIヒントの開閉は維持。
- 主入口からの移動は長いページの中央ではなく先頭へ。固定ヘッダー用の余白を付け、履歴の見出し・カレンダーから見えるようにした。

## 変えていないこと

Auth、対象者/家族権限、AIへの送信条件、相談記憶、クラウド保存、復元、手帳統合、日記保存/編集/削除の処理は変更していない。
クラウドの状態・失敗理由・本人確認・入力途中の保護・削除確認を非表示にしていない。
本番DB操作、メール/招待送信、AIへの相談送信、環境設定変更、利用者データの移動/上書き/削除はしていない。

## 検証結果

- ソース回帰29/29 PASS。新Bテストは実JSX・実ハンドラーを抽出して0/1/2件の記録、3入口、同名の別対象者、最新記録、6タブ、書類へのフォーカス、null対象者を検査。実ブラウザの代替ではない。
- 新テーマCSSを構文解析し、既存CSSより後に読み込むこと、代表色の7:1以上、本文/入口/補助操作サイズ、スクロール余白を検査。
- Web lint・型チェック・Next production build PASS。既存のimg/React hook依存警告あり。依存パッケージの更新・自動installなし。
- ローカルは実行時環境変数をallowlist化し、dotenvがないことを確認して起動。本番の認証情報を読み込まず、DB/APIキー未設定のままテストした。
- CUA実ブラウザで架空の対象者を登録。必須4項目の空欄エラー、日記の空欄エラー、記録作成→保存表示→履歴→編集保存→再読み込み後の本文保持を確認。
- 編集欄をキーボードで空にすると保存無効。変更せず閉じると元の本文が残ることを確認。
- 3入口から記録フォーム/履歴/対象者に対応するAI画面へ到達。AI画面は環境未設定を明示して送信を止める既存の安全動作を確認（AI回答生成は未検証）。
- 書類の場所ボタンで `document-location-note` にフォーカス。家族画面へのリンクも到達。招待・メールは送っていない。
- 手帳の横幅320/360/390/736/1280pxでscrollWidthと画面幅が一致。本文20px・700・`rgb(41,38,31)`をDOMで確認。390pxと320pxの画面を目視確認。
- モニター回答プレビューと特商法ページは320px、家族/AI画面は360pxで横はみ出しなし。回答プレビューは新書体を使用し、送信不可のまま。モニターの実送信/7日間の実経過は再実施していない。
- production buildをローカル起動し、Bホームが描画されること、以前の架空記録が保持されること、当該タブの最終console error/warnが0件であることを確認。

## 次の受入

このパソコンで `http://127.0.0.1:3119/home` を確認用に起動する。スマホで開く本番URLではない。
実iPhone/Safari、200%拡大、全画面の全状態監査、本番の認証済み保存/家族操作はこのデザイン作業では未実施。
以前のStage A（別端末復元・写真・家族招待権限）の残項目も継続。
利用者の見た目確認と必要な受入を経て、本番反映する。
