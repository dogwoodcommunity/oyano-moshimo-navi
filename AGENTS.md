# 共通の作業ルール（Codex / Claude Code）

## 必要な情報だけ読む

- 再開時は `docs/CURRENT_STATUS.md` とGitのbranch/statusを確認する。小さな継続修正で既読情報を繰り返し取得しない。
- `docs/SESSION_HANDOFF.md` は履歴。見出しを `rg -n` で探し、今回に関係する節だけ読む。
  2026-09-19の本人の省トークン方針により、従来の「毎回全文読了」はこの参照方式へ変更する。
- 変更先に適用される指示と、選んだスキルの必須手順は省略しない。関係のない資料・スキルは読み込まない。
- 構成は `apps/web`（Web）、`apps/mobile`（モバイル）、`packages`（共通）、`supabase`（SQL）、`scripts`（検証）。
  全体設計は `docs/PRODUCTION_ROADMAP.md`、公開は `docs/PRODUCTION_CHECKLIST.md`、
  個人情報保管は `docs/PERSONAL_DATA_PRODUCTION_ARCHITECTURE_2026-09-08.md` を必要な作業時だけ参照する。

## 作業と検証

- 目的・変更範囲・完了条件を短く定め、依頼範囲内の編集と安全なローカル検証は続けて完了させる。
- テストは変更の影響範囲に合わせる。公開・保存/復元・認証/権限・DB変更では必要な回帰確認を省かない。
  `.github/workflows/ci.yml` と関連する `scripts/test-*.mjs` で実行条件・副作用を確かめる。
- 並列化は独立した作業で時間/品質面の効果がある場合に限定し、短い依頼と必要な情報だけを渡す。
  同じファイルを同時編集しない。Claude等の外部レビューへ秘密情報や実利用者データを送らない。
- ログは要点・失敗箇所を取得する。同じ差分の重複レビュー、不要な全件再検証、長い途中報告を避ける。

## GPT-6 Sol / Astraの使い分け（2026-09-23 本人指定）

### 基本とSolで進める範囲

- 通常はGPT-6 Sol。品質・安全性・完成基準を維持し、不要な利用枠消費と停滞を減らすための追加ルール。
  既存の仕様、セキュリティ、独立レビュー（Claude等）、テスト、承認、Git記録、並列作業の要件を削除・緩和しない。
- 作業内容・影響範囲・不確実性・テスト結果からエージェントが切替要否を判断する。ファイル数/コード量だけで判断せず、
  毎回本人にモデル選択を丸投げしない。着手時は「次工程のモデル判断・理由・具体的な次作業」を短く報告する。
- Solの範囲: 仕様/完了条件が明確な実装、画面/フォーム/操作性/文言改善、原因と影響範囲が特定済みの不具合修正、
  テスト作成/実行/既存機能確認、設計を変えない整理/文書更新、Astraで設計・範囲・検証項目を確認済みの実装。
  例えば認証画面の文言変更と認証方式の変更を区別する。切替不要で許可済みの作業は不要に停止しない。
- モデルの選択操作は本人が行う。モデル/思考設定/利用プラン/課金方法を無断変更しない。
  現在のモデルを環境から確認できなければ推測しない。実際に変更していないのに切替済みと報告したり、
  別モデルになったつもりで作業しない。切替時は本人の完了返答を前提に再開する。
  切替の代わりに、無断で別API・有料サービス・別モデルのエージェントを呼び出さない。

### Astraへ切り替える条件

以下に該当すれば、判断に必要な読取調査だけ行い、対象変更の着手前に止めて切替を案内する。

1. 認証/権限、個人情報・本人確認画像の保管/削除、秘密情報、決済/返金/報酬計算など、重要な仕組みの新設・設計変更。
2. 全体設計、主要データ構造、複数機能に共通する処理、大規模移行など影響が大きい変更、または影響範囲が不明。
3. 原因の根拠不足、仕様矛盾、重要な前提を推測で決める必要など、重大な不確実性がある。
4. 同じ問題で、仮説と検証を伴う修正が2回連続失敗。重大リスクなら2回を待たない。根拠のない試行はしない。
5. Solで実装した認証/権限/個人情報/金銭処理等の重要処理を、統合・公開前にレビューする。
6. 初回公開、主要機能追加、重要処理変更を含む本番リリースの最終レビュー。
   単なる文言修正まで一律に全体レビューを繰り返す必要はない。

列挙外でも重大な不確実性が解消できなければ案内する。自信を理由に必須確認を省かない。

### Astraへの引き継ぎと停止

- 案内の最初の一文は「ここから先はGPT-6 Astraに切り替えてください。」とする。
- 続けて以下の項目を簡潔に残す。コード/ログ全文を貼り直さず参照先を示し、秘密情報/個人情報を含めない。
  - 【切り替え理由】該当条件と必要な判断/確認。
  - 【現在の状態】目的、完了済み、未完了。
  - 【変更箇所】関係ファイル、差分、未コミット変更、判明しているbranch/commit。
  - 【検証状況】テストと結果、再現手順、未検証事項。事実と仮説を分ける。
  - 【Astraへの依頼】次の判断/作業、Solへ戻せる条件。
- 作業中の引き継ぎは既存の進捗/引き継ぎファイルへ保存し、AGENTS.mdに蓄積しない。
  引き継ぎを残したら応答を終了。本人の「切り替えた。続けて」等の完了返答まで対象作業を進めず、
  並列担当にも停止を適用する。停止後に裏で作業を続けているような報告もしない。

### Solへ戻す判断と引き継ぎ

- Astraは切替理由となった設計判断/原因究明/問題解決/レビューに集中し、周辺機能追加や無関係な全面改修をしない。
- 重要な不確実性が解消し、残りの作業範囲と検証方法が明確なら「ここからはGPT-6 Solに戻して進められます。」と案内する。
  確認/決定事項、Solの実装/検証範囲、変更不可の前提、必要テスト/完了条件、再びAstraが必要になる条件を残す。
  応答を終了し、本人の切替完了返答後に再開する。
- 確認済み範囲の実装で同じ理由の切替を何度も要求しない。前提変化や新たな重大問題があれば再判断する。

### 節約と完了の境界

- 上記「必要な情報だけ読む」「作業と検証」に従い、関係する指示/記録/差分/ファイルから必要に応じ調査を広げる。
  理由のない全体再読、同じ調査/長い説明、無関係なリファクタリング、将来のためだけの過剰設計をしない。
- 節約のためにテスト/セキュリティ確認/必要レビューを省略しない。レビュー/テスト/保存/commit/pushは実行分のみ完了と報告。
  テストPASSやAstraの確認だけを根拠に、無条件に安全・完成とは判断しない。
- モデル切替後の「続けて」は本番反映/実データ削除/実決済等への包括承認ではない。既存の承認手順を守る。

## 守ること・引き継ぎ

- `review_exports/`、未追跡の `docs/CLAUDE_FULL_REVIEW_{REQUEST,RESULT}_2026-09-03.md`、利用者の既存変更を触らない。
- 本番変更、データ削除、外部送信、契約/課金は現在の依頼で承認された範囲だけ行う。過去の承認を拡張しない。
- ローカルPASS・CI成功・本番反映・実データでの確認を区別する。秘密値をログやGitに残さない。
- 完了時は `CURRENT_STATUS.md` を短く更新し、`SESSION_HANDOFF.md` へ新しい差分・根拠だけ追記する。
  対象差分とブランチ、公開への連動を確認してGitHubへpush。文書のみなら `[skip ci]` を用い、再デプロイしない。
